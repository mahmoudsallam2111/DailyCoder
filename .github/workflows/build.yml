name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - master

env:
  DOTNET_VERSION: "9.x"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: DailyCoder

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore DailyCoder.slnx

      - name: Build
        run: dotnet build DailyCoder.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test DailyCoder.slnx --configuration Release --no-restore --no-build

      - name: Publish
        run: dotnet publish DailyCoder.slnx --configuration Release --no-restore --no-build
